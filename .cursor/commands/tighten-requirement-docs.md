Following everything we have learnt, any updates, clarifications or tightening you recommend to the PRD and the TRD to ensure this issues don't pop up again should the system be re-implemnted again from those docs?

As you do this, please remember: 

PRDs MUST contain only intent-level contracts.
- Technical design details MUST live in the TRD / Tech Spec.
PRD's SHOULD NOT include:
  - exact file paths and file naming rules
  - exact config schema fields
  - exact prompt or schema implementation details
  - retry algorithms, merge algorithms, or discovery algorithms
  - provider-specific API wiring details
